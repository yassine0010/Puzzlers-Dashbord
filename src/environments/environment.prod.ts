export const environment = {
  production: true,
  // Option 1: Use relative URL if using Vercel proxy (recommended)
  apiUrl: '/api',

  // Option 2: Direct API URL (uncomment and update with your actual API URL)
  // apiUrl: 'https://your-production-api.com/api',

  // Option 3: Azure/AWS API Gateway (uncomment if using)
  // apiUrl: 'https://your-api-gateway.azure-api.net/api',
};
